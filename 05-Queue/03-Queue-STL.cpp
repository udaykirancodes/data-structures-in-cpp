#include<bits/stdc++.h>
using namespace std; 
int main(){

	// queue init 
	queue<int> q;

	// size 
	int size = q.size(); 

	// push 
	q.push(10);

	// pop 
	q.pop(); 

	// front element 
	q.front(); 

	// is Empty 
	bool empty = q.empty();

	return 0;
}