#include<bits/stdc++.h>
using namespace std; 
int main(){
	// init 
	stack<int>s;

	// size 
	int size = s.size(); 

	// top 
	int top = s.top();

	// is Empty 
	bool empty = s.empty();

	// Push 
	s.push(10);

	// Pop
	s.pop();

	return 0;
}